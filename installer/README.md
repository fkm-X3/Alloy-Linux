Installer

Text-based installer prototype and scripts. Installer should create partitions, install base system, and configure bootloader.

Primary install target is Orange Pi 5 (RK3588) with a U-Boot boot flow.
