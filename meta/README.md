Meta

Release metadata, manifests, and target profiles.

- `versions.yaml`: pinned kernel/toolchain versions used by build scripts.
- `targets/orangepi5.yaml`: Orange Pi 5 platform profile (boot + layout assumptions).

Keep signing keys out of the repository; store references to secure keystore.
