CI

CI pipeline definitions and reproducible-build manifests go here.

- `reproducible-check.sh`: validates pinned manifests are present.
- `qemu-smoke.sh`: ARM64 smoke-test hook for CI environments with QEMU available.
